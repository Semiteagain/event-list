import React from 'react'

function eventList(props) {
    return (
        <div>
            <h3>{props.title}</h3>
            <p>{props.content} </p>
            <input type="date" name="" id=""/>
        </div>
    )
}

export default eventList
